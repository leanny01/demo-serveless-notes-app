const source = {
    source:{
      email:"dibwe.kalangu@gmail.com",
      storage:21,
    },
    
  }

  // stringify the source object
    const sourceString = JSON.stringify(source);
    console.log(sourceString);